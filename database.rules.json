{
  "rules": {
    "typing": {
      "$conversationId": {
        ".read": "auth != null",
        "$userId": {
          ".write": "auth != null && auth.uid == $userId",
          ".validate": "newData.isNumber() || newData.val() == null"
        }
      }
    }
  }
}
